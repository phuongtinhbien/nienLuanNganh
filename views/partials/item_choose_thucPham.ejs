<style>
.shadow{
    border-color:transparent; border-radius:15px; box-shadow: 0px 1px 10px rgba(0, 0, 0, .25);
}
</style>


<div class="card shadow mt-2">
    <div class="d-flex flex-row">
        <div class="p-2 w-50 p-3">
        <div class="d-flex align-items-center">
                <div class="custom-control custom-checkbox">
                        <input type="checkbox" name= "checked[]" onchange="update()"checked class="custom-control-input" id="l<%-data.id%>" value='<%-JSON.stringify(data)%>'>
                        <label class="custom-control-label ml-1" for="l<%-data._id%>">
                                <span class="card-title display-4"  style="text-transform:uppercase;font-weight:bold; font-size:16px"><b><%-data.name%></b></span>
                        </label>
                </div>
            </div>
            

        </div>
        
        <div class="p-2 w-50 p-3">
                <div class="d-flex align-items-center">
                    <span   style="text-transform:uppercase">
                    <input type="number" name="" id="<%-data._id%>" oninput="update()" class="form-control" value="100" min="0" max="5000" step="2" placeholder="Khối lượng (g)" required="required" title="">
                     </span>
                </div>
        </div>
        <div class="p-2 w-25 p-3">
        <a class="btn btn-success" href="/food/<%-data._id%>" style="border-radius: 25px 25px 25px 25px">Chi  tiết</a>
    </div>
    </div>
</div>

